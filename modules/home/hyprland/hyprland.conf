# Haoiiii :3c
#
# the good shit:
# https://wiki.hyprland.org/Configuring/
#
#
source = ~/.config/hypr/Keybinds.conf
source= ~/.cache/wal/colors-hyprland.conf

env = XDG_CURRENT_DESKTOP,Hyprland


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/

#This is specific to my personal PC setup, and will unlikely conflict with any user of this config
monitorv2 {
output = desc:ASUSTek COMPUTER INC ASUS VG249 0x00009A40
mode = 1920x1080@144
position = 0x0
scale = 1
vrr = 1 #Variable refresh rate
}

monitorv2 {
output = desc:Acer Technologies SB220Q 0x203022C0
mode = 1920x1080@60
position = auto-center-left
scale = 1
vrr = 0
}
#second monitor
monitorv2 {
output = desc:Panasonic Industry Company 11SP_HTIB
mode = highres
position = auto-center-up
scale = 2
vrr = 1  
}

# Wildcard monitor activator
monitor=,preferred,auto,auto

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = waybar &

# Makes the desktop get dimmer the later in the day it is aiding sleep
exec-once = hyprsunset

# Makes the screen turn off and probes hyprlock
exec-once = hypridle

# Notif Daemon 
exec-once = dunst 

# Raina wuz here ilysm mal
exec-once = [workspace 2 silent] signal-desktop

# Authentication and Privilage escalation magic
exec-once = dbus-update-activation-environment --systemd --all
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = gnome-keyring-daemon --start --components=secrets

#hotplug safe monitor event checker to see if my unique monitor config is detected otherwise this never does anything. 
#exec-once = socat -u UNIX-CONNECT:$XDG_RUNTIME_DIR/hypr/$HYPRLAND_INSTANCE_SIGNATURE/.socket2.sock - | while read -r line; do case "$line" in monitoradded*) ~/.config/hypr/scripts/unique-monitor-config.sh ;; esac done
# The above script but runs on startup rather than being a backround process
exec-once = ~/.config/hypr/scripts/unique-monitor-config.sh 

# Backround and pywal color script
exec-once = ~/./.config/hypr/scripts/Wallpaper.sh &

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = GTK_THEME,Wal

xwayland {
   force_zero_scaling = true
}

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    # Gaps and border
    gaps_in = 4
    gaps_out = 5
    gaps_workspaces = 50
    border_size = 1
    
    # Fallback colors
    col.active_border = $color2
    col.inactive_border = $background

    resize_on_border = true
    no_focus_fallback = true
    layout = dwindle
    
    #focus_to_other_workspaces = true # ahhhh i still haven't properly implemented this
    allow_tearing = true # This just allows the `immediate` window rule to work
}
# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 0    
    blur {
        enabled = false
        xray = true
        special = false
        new_optimizations = false 
        size = 6 
        passes = 1
    #noise = 0.01
    #contrast = 1
    #popups = true
    #popups_ignorealpha = 0.6
    }
    
    # Shadow
    shadow {
        enabled = true
        ignore_window = true
        range = 20
        offset = 0 2
        render_power = 4
        color = rgba(0000002A)
    }

    # Window Opacities
    # active_opacity = 1
    # inactive_opacity = 1
    # fullscreen_opacity = 1
    
    # Shader
    # screen_shader = ~/.config/hypr/shaders/nothing.frag
    # screen_shader = ~/.config/hypr/shaders/vibrance.frag
    
    # Dim
    dim_inactive = false

  #dim_strength = 0.1
  #dim_special = 0
}

# https://wiki.hyprland.org/Configuring/Variables/#animations

animations {
    enabled = true 
    # Animation curves
    
    bezier = linear, 0, 0, 1, 1
    bezier = md3_standard, 0.2, 0, 0, 1
    bezier = md3_decel, 0.05, 0.7, 0.1, 1
    bezier = md3_accel, 0.3, 0, 0.8, 0.15
    bezier = overshot, 0.05, 0.9, 0.1, 1.1
    bezier = crazyshot, 0.1, 1.5, 0.76, 0.92 
    bezier = hyprnostretch, 0.05, 0.9, 0.1, 1.0
    bezier = menu_decel, 0.1, 1, 0, 1
    bezier = menu_accel, 0.38, 0.04, 1, 0.07
    #bezier = easeInOutCirc, 0.85, 0, 0.15, 1
    bezier = easeOutCirc, 0, 0.55, 0.45, 1
    bezier = easeOutExpo, 0.16, 1, 0.3, 1
    bezier = softAcDecel, 0.26, 0.26, 0.15, 1
    bezier = md2, 0.4, 0, 0.2, 1 # use with .2s duration
    # Animation configs
    animation = windows, 1, 3, md3_decel, popin 60%
    animation = windowsIn, 1, 3, md3_decel, popin 60%
    animation = windowsOut, 1, 3, md3_accel, popin 60%
    animation = border, 1, 10, default
    animation = fade, 1, 3, md3_decel
    #animation = layers, 1, 2, md3_decel, slide
    #animation = layersIn, 1, 3, menu_decel, slide
    animation = layersOut, 1, 3, menu_accel
    animation = fadeLayersIn, 1, 2, menu_decel
    animation = fadeLayersOut, 1, 0.5, menu_accel
    animation = workspaces, 1, 7, menu_decel, slide
    animation = workspaces, 1, 1, softAcDecel, slidefade
    animation = specialWorkspace, 1, 3, md3_decel, slidevert
}


# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
misc {
    vfr = 1
    vrr = 1
    animate_manual_resizes = false 
    animate_mouse_windowdragging = false
    enable_swallow = false
    swallow_regex = (foot|kitty|allacritty|Alacritty)
    
    disable_hyprland_logo = true
    force_default_wallpaper = 0
    allow_session_lock_restore = true
    
    initial_workspace_tracking = false
}


# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}

# Overview
plugin {
    hyprexpo {
        columns = 3
        gap_size = 5
        bg_col = rgb(000000)
        workspace_method = first 1 # [center/first] [workspace] e.g. first 1 or center m+1

        gesture_positive = false
    }
}
##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*
# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

